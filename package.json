{
  "name": "obsidian-local-sage",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered local tools for Obsidian - monorepo containing CLI and Obsidian plugin",
  "repository": {
    "type": "git",
    "url": "https://github.com/kakao-lucas-ms/obsidian-local-sage.git"
  },
  "author": "kakao-lucas-ms",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
